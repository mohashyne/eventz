<%#to use the local browser for submission we set the 
 loccal to true , local: true annd that  disable ajx%>
<%= form_with(model: event, local: true) do |f| %>
<%# <%- event.errors.full_messages.to_sentence %>

 <%# <%= render 'shared/error_messages', object: f.object %>
                    
                        <%# OR %>
  <%= render 'shared/errors', object: event %>

<%= f.label :name %>
<%= f.text_field :name, autofocus: true %>

<%= f.label :description %>
<%= f.text_area :description, rows: 5 %>

<%= f.label :location %>
<%= f.text_field :location %>

<%= f.label :price %>
<%= f.number_field :price %>

<%= f.label :starts_at %>
<%= f.datetime_select :starts_at, {ampm: true}, {class: "datetime"} %>

<%= f.label :capacity %>
<%= f.number_field :capacity %>

<%= f.label :image_file_name %>
<%= f.file_field :image_file_name %>

<%# mind u the  submit button will work using the Ajx method in javascript %>
<%# <%= f.submit "Update", class: "button" %>

<%# the button an name will be assigned automatically if we use tthe local: true %>
<%= f.submit %>
<% end %>





